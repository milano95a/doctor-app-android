<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.tech.dst.doctorapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity android:name=".LoginActivity" android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity" />

        <activity android:name=".CommunicationListActivity" />

        <activity android:name=".ChatActivity" />

        <activity android:name=".PhysicalMonitoringActivity" />

        <activity android:name=".HospitalChartActivity" />

        <activity android:name=".SearchClinicsActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity android:name=".KnowledgeBaseActivity" />

        <activity android:name=".PersonalNoteActivity" />

        <activity android:name=".ClinicInfoActivity" />

        <activity android:name=".MyProfileActivity" />

        <activity android:name=".SettingsActivity" />

        <activity android:name=".LoginFormActivity" />

        <activity android:name=".ArticleInfoActivity" />

        <activity android:name=".FavoriteListActivity" />

        <activity android:name=".DoctorProfileActivity" />

    </application>

</manifest>